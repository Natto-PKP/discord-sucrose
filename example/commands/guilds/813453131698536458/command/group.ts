import type { ChatInputSubCommandGroup } from '../../../../../typings/index';

export default <ChatInputSubCommandGroup>{
  option: {
    name: 'group',
    description: 'Command with group of sub commands',
  },
};
